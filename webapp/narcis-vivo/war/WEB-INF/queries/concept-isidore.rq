PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
select * where {
	?c a skos:Concept.
	?c skos:inScheme <http://www.rechercheisidore.fr/categorie>.
	?c skos:prefLabel ?l.
	filter(lang(?l) = 'en')
}

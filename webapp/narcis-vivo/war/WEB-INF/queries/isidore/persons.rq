PREFIX foaf: <http://xmlns.com/foaf/0.1/>
select ?resource ?firstName ?lastName where {
?resource a foaf:Person.
?resource foaf:givenName ?firstName.
?resource foaf:familyName ?lastName.
filter(str(?firstName) != "")
}
